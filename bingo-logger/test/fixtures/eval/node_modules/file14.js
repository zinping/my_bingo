const bingo = require("../../../../");

module.exports = function() {
    const logger = bingo(
        bingo.transport({
            target: 'bingo/file'
        })
    )

    logger.info('done!')
}
